#ifndef SRT_H
#define SRT_H

/* void process_sort_by_remain(Process *p, int n) {

    // sort by remain

    int j;
    for (int i = 0; i < n; i++) {
        Process t = p[i];
        for(j = i - 1; j >= 0 && p[j].remain > t.remain; j--)
            p[j + 1] = p[j];  // move right record
        p[j+1] = t;
    }

} */

#endif